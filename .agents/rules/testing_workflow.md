# Testing Workflow Rule

Do not perform automatic tests (such as browser subagent testing, end-to-end tests, or automated test runners) by default.
Always ask the user for permission before running tests.

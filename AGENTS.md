# Agent Guidelines & Workflow Rules

- **Testing Workflow**: Do not perform automatic tests (such as browser subagent testing, end-to-end tests, or automated test runners) by default. Always ask the user for permission before running tests.
- **Git Workflow**: Do not commit or push to remote repositories automatically. Always ask the user for permission before committing or pushing changes.
